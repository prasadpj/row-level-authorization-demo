var config = {
    
    host: 'localhost',

    port: 3001,

    mongo: {
        host: "localhost",
        port: 27017,
        db: "MoviesDB"
    },
    EncryptionKey: "TheKey123",
};

module.exports = config;